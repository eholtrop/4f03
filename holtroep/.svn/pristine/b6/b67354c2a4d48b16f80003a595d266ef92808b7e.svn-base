//Generates a random matrix at the pointer A
void genMatrix(int m, int n, double *A)
{
	int i = 0;
	for(i = 0; i < m*n; i ++)
	{
		A[i] = drand48();
	}
}

//Generates a random Vector at the pointer b
void genVector(int n, double *b)
{
	int i = 0;
	for(i = 0; i < n; i++)
	{
		b[i] = drand48();
	}
}
